targets:
  $default:
    builders:
      json_serializable:
        options:
          # The API speaks snake_case; Dart fields stay camelCase.
          field_rename: snake
          explicit_to_json: true
